%h1 Orders
%ul
  - @orders.each do |order|
    %li.list-group
      .list-group-item
        %h3
          Order ID: 
          = order.id
          .float-right
            Total: ₹
            = order.cart.total 
        %b Email: 
        = order.admin.email
        %br
        %b Address: 
        = order.address

      %ul
        %tt
          - order.cart.cart_items.each do |cart_item|
            %li.list-group-item
              = cart_item.menu_item.name
              .float-right
                Quantity: 
                = cart_item.quantity
                Subtotal: 
                = cart_item.sub_total
